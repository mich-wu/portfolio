import React from 'react';
import cn from 'classnames';
import style from './templateX.mod.scss';

const TemplateX = ({}) => {
  const classes = cn(style.templateX);

  return <div className={classes}></div>;
};

export default TemplateX;
